inicioprog

inicioargs
	literal letra;
	inteiro cont;
fimargs

{ Declaracao de variaveis }
iniciovars
	literal chute;
	inteiro zero;
fimvars

chute = "k"; { Caractere aleatorio }
zero = 0;

se (letra == chute) entao
	escreva "Acertei!\n";
	{ Contador menor que zero nao eh aceito }
	se (cont < zero) entao
		escreva "cont < 0\n";
	fimse
fimse

enquanto (cont > 0) faca
	escreva letra;
	escreva "\n";
fimenquanto

fimprog
